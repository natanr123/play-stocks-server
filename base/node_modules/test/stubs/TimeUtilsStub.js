import sinon from 'sinon';
import TimeUtils from 'app/service/TimeUtils';

export default class TimeUtilsStub
{
    static was_wrapped = false;
    static stub = null;
    static start() {
        if(TimeUtilsStub.stub!==null) {
            return;
        }
        TimeUtilsStub.stub = sinon.stub(TimeUtils, 'isNYSEopen',()=>{
            let response = true;
            console.log('TimeUtilsStub isNYSEopen is running response: ',response);
            return response;

        });
    }

    static stop() {
        // if(TimeUtilsStub.stub===null) {
        //     return;
        // }
        // TimeUtilsStub.stub.restore();
        // TimeUtilsStub.stub = null;
    }

}